import styled from "styled-components";
 
    const Nav = styled.section`
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 3% 0;
    `;

    export default Nav;